name: Trigger Jenkins Build

on:
  push:
    branches: [ master ]

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins
        run: |
          curl -X POST https://6257177de7cf.ngrok-free.app/job/product-service-cicd/build
